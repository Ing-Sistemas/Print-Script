package org.example.parser.syntactic.builder

import Token
import org.example.ASTNode

class UnaryNodeBuilder : ASTBuilderStrategy {
    override fun build(token: Token, tokens: List<Token>): ASTNode {
        TODO("not implemented")
    }
}