#!/bin/sh

# formatea el codigo
./gradlew formatApply

# corre los tests
./gradlew test

# si falla, rechaza el commit
if [ $? -ne 0 ]; then
  echo "Pre-commit checks failed. Commit cancelled."
  exit 1
fi
