package output

import interfaces.OutPut

class Conditional : OutPut {
    override fun output(code: Any) {
        if (code == true) {
            // interpret expression in if¿?
            TODO()
        }
    }
}